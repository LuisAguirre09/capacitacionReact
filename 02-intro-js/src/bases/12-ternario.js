
const activo = true;

const mensaje1 = (activo) ? 'Activo' : 'Inactivo'; //operador ternario

//const mensaje = ( !activo ) && 'Negativo'; //operador ternario abreviado (retorna false)
const mensaje2 = ( activo ) && 'Activo'; //retorna "activo"

console.log(mensaje);